<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Actions -->
        <record id="hotel_service_action" model="ir.actions.act_window">
            <field name="name">Services</field>
            <field name="res_model">hotel.service</field>
            <field name="view_mode">tree,form</field>
        </record>

        <!-- Tree View -->
        <record id="hotel_service_tree" model="ir.ui.view">
            <field name="name">hotel.service.tree</field>
            <field name="model">hotel.service</field>
            <field name="arch" type="xml">
                <tree>
                    <field name="name"/>
                    <field name="price"/>
                </tree>
            </field>
        </record>

        <!-- Form View -->
        <record id="hotel_service_form" model="ir.ui.view">
            <field name="name">hotel.service.form</field>
            <field name="model">hotel.service</field>
            <field name="arch" type="xml">
                <form>
                    <sheet>
                        <group>
                            <field name="name"/>
                            <field name="price"/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>
    </data>
</odoo>